# TIMELINE — <project name>

The chronology of stages in broad strokes: a view from above, so the state of things is clear in a minute.
One row = one stage from PLAN.md. Details go in WORKLOG.md, tasks in PLAN.md. Don't duplicate them here.

Statuses: `not started` · `in progress` · `completed` · `blocked` · `cancelled`

| Stage | Status | Started | Completed | Summary | Comment |
|---|---|---|---|---|---|
| 1. <name> | not started | — | — | — | |
| 2. <name> | not started | — | — | — | |

A closed stage carries a link to `stages/<ID>-<name>.md` and the git tag `stage/<ID>`.

---

**Current position:** <the stage and task being worked on right now>
**Next step:** <what comes next in the plan>
