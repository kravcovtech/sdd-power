# SDD Power

*[English version](README.en.md)*

Скилл для Claude Code: дисциплина длительной разработки по существующей проектной документации — с персистентным трекингом состояния и критической проверкой самой документации.

Он нужен там, где работа не укладывается в одну сессию: агент возвращается к проекту через неделю с пустым контекстом и должен за минуту понять, что сделано, что осталось и почему когда-то приняли именно это решение.

## Зачем это нужно

Скилл лечит две болезни долгой разработки с агентом.

**Потеря состояния между сессиями.** Контекст обнуляется. Через неделю ни агент, ни человек не помнят, на чём остановились и почему выбрали такой подход. Лечится четырьмя файлами трекинга, которые обновляются в тот же момент, что и код — не «потом».

**Слепое доверие документации.** Документацию писали люди, она устаревает и содержит ошибки. Агент, исполняющий ошибочную доку буквально, производит баги, которые потом трудно объяснить. Лечится протоколом проверки с объективным критерием: ошибка — только то, что можно доказать фрагментом кода, двумя противоречащими абзацами или ошибкой выполнения. «Я бы сделал иначе» ошибкой не является и идёт в «Предложения по улучшению».

Обе части работают только вместе с дисциплиной. Одно «допишу потом» — и файлам больше нельзя верить, а значит, они бесполезны.

## Как это устроено

Состояние живёт в `docs/sdd-power/`. Роли файлов не пересекаются — это защита от того, чтобы одно и то же расползлось по трём местам:

| Файл | Отвечает на вопрос | Природа |
|---|---|---|
| `PLAN.md` | Что предстоит сделать? | Живой документ, переписывается |
| `WORKLOG.md` | Что и почему было сделано? | Append-only, новые записи сверху |
| `TIMELINE.md` | Где мы в целом? | Таблица этапов |
| `LESSONS.md` | На чём мы уже обжигались? | Курируемый, жёсткий бюджет |
| `stages/<ID>.md` | Что представляет собой закрытый этап? | Пишется один раз при закрытии |

Директория по умолчанию попадает в `.gitignore`: это рабочая память агента, а не артефакт репозитория — ей не место в диффах PR. Следствие учтено в самом скилле: всё, что важно команде, обязано доехать до кода, документации или CLAUDE.md, а не остаться только в WORKLOG. Мостом между журналом решений и историей репозитория служит короткий SHA коммита в каждой записи.

Несколько решений, которые отличают это от «веди TODO-файл»:

- **Покрытие проверяется в обе стороны.** Каждая задача ссылается на раздел документации, и каждое требование документации либо покрыто задачей, либо явно вынесено в «Вне объёма» с причиной. Иначе потерянное требование всплывает в самом конце.
- **DoD этапа проверяется командами**, а не суммой галочек. Красный прогон — это цикл «чини и перегоняй», а не вердикт: закрытого этапа с красным DoD не существует.
- **`[x]` только при наблюдаемом подтверждении.** «Должно работать» и «изменение тривиальное» доказательствами не считаются — иначе файлы начинают врать, а цена вранья в них нулевая.
- **LESSONS.md — правила, а не журнал багов.** «Баг #14 закрыт 12.03» ничему не учит. «Не буферизуй полный результат перед экспортом — OOM на отчётах >10MB, используй стриминг» — применимо к новому коду. Бюджет — 20 правил и ~120 строк, потому что файл читается целиком каждую сессию.
- **Сильнейшая консолидация — превратить правило в проверку.** Что ловится тестом, линтером или CI, не должно жить текстом в расчёте на память будущей сессии.
- **Снимок документации датирован.** Дока изменилась — ре-синхронизация плана, а не работа по памяти.
- **Скоуп держится.** Замеченное попутно дублирование и мёртвый код идут задачей в PLAN.md, а не в текущий дифф. Исключение — если находка физически блокирует задачу.
- **Инфраструктура чинится сама.** Проект, начатый более ранней версией скилла, при следующем запуске молча приводится к текущей: файлы переезжают в `docs/sdd-power/`, добавляются `stages/`, строка в CLAUDE.md и запись в `.gitignore`. Старые записи не переписываются.

## Чего он не делает

- Это дисциплина, а не автопилот. Файлы ценны ровно настолько, насколько честно они заполняются; агент, который поставил `[x]` без проверки, ломает систему быстрее, чем её отсутствие.
- Он не заменяет документацию и не пишет её за вас. Нет внятного источника требований — скиллу не на чем строить план.
- Он не решает архитектурные развилки сам. Вопросы, чей ответ меняет модель данных, эскалируются пользователю до начала кода.
- Он избыточен для разовой мелкой правки в одну сессию — для неё разворачивать церемонию не нужно.
- Трекинг не переживёт свежий клон и не виден коллегам: он намеренно вне git.

## Установка

```bash
git clone https://github.com/kravcovtech/sdd-power.git ~/.claude/skills/sdd-power
```

Windows (PowerShell):

```powershell
git clone https://github.com/kravcovtech/sdd-power.git $env:USERPROFILE\.claude\skills\sdd-power
```

Для одного проекта — клонируйте в `.claude/skills/sdd-power` внутри репозитория.

Английская версия лежит в `en/` и ставится копированием этой директории — см. [README.en.md](README.en.md).

Перезапустите Claude Code. Скилл подхватится сам на фразах вроде «начни разработку по документации» или «продолжи с места остановки», а также если в проекте уже есть `docs/sdd-power/`.

## Как использовать

**Первая сессия** — инициализация:

> Начни разработку по документации в `docs/`

Агент прочитает документацию критическим взглядом, создаст `docs/sdd-power/`, разложит требования на этапы и задачи, покажет найденные расхождения и открытые вопросы — и дождётся вашего подтверждения плана до первой строки кода.

**Любая следующая сессия** — продолжение:

> Продолжи разработку

Агент прочитает файлы трекинга, сверит их с фактическим состоянием кода (файлы могут врать — работу могли делать мимо него), разберёт незакрытые `[~]` и в двух предложениях скажет, где остановились и что идёт следующим.

Полезные фразы: «сверься с планом», «зафиксируй прогресс», «где мы остановились», «закрой этап».

## Структура репозитория

```
.
├── README.md                         # русская версия
├── README.en.md                      # английская версия
├── LICENSE
├── SKILL.md                          # ядро: режимы, протокол, правила обновления
├── assets/                           # шаблоны файлов трекинга
│   ├── PLAN.template.md
│   ├── WORKLOG.template.md
│   ├── TIMELINE.template.md
│   ├── LESSONS.template.md
│   └── STAGE.template.md
├── references/
│   └── docs-verification.md          # пограничные случаи: ошибка доки или вкусовщина
└── en/                               # самодостаточная английская копия скилла
    ├── SKILL.md
    ├── assets/
    └── references/
```

Русская версия лежит в корне, чтобы обычный `git clone` в `~/.claude/skills/` ставил её напрямую; `en/` — полная копия того же скилла на английском.

`references/docs-verification.md` подгружается только при сомнении — тест на объективность, каталог типов ошибок с примерами, разбор пограничных случаев (дока требует небезопасного; дока противоречит рабочему коду; ошибка найдена в уже реализованной части) и формат эскалации.

## Планы

- [ ] Команда для быстрой ревизии трекинга на рассинхрон с кодом
- [ ] Пример заполненного `docs/sdd-power/` на демонстрационном проекте
- [ ] Вариант с трекингом внутри git — для команд, которым состояние нужно общим

## Как помочь проекту

Система развалилась на вашем проекте, файлы разошлись с кодом или протокол проверки документации дал ложное срабатывание? Откройте issue с конкретным сценарием или пришлите pull request. Особенно ценны случаи, где дисциплина ломается на практике — именно из них берутся правила. Русскую и английскую версии держите синхронными.

## Лицензия

[MIT](LICENSE) — можно использовать свободно, в том числе в коммерческих проектах. Указание авторства приветствуется, но не обязательно.

---

Поддерживает [@kravcovtech](https://github.com/kravcovtech).
